<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.hang.data.mapper.TeacherMapper">

    <resultMap id="teacherMap" type="top.hang.data.entity.Teacher">
        <!--    column都是数据库里面的 property都是自定义的    -->
        <id column="teacher_id" property="teacherId"/>
        <result column="teacher_name" property="teacherName"/>
        <!--      association 一对一别名(mybatis自带)  -->
        <association property="clazz" javaType="top.hang.data.entity.Clazz">
            <id column="clazz_id" property="clazzId"/>
            <result column="clazz_name" property="clazzName"/>
        </association>
    </resultMap>


    <select id="selectOneByOne" resultMap="teacherMap" parameterType="java.lang.Integer">
        SELECT *
        FROM t_teacher t,
             t_clazz c
        WHERE t.teacher_id = #{teacherId}
          AND t.clazz_id = c.clazz_id
    </select>


</mapper>